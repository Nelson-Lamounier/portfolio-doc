/**
 * Ambient type declarations for modules that do not ship their own .d.ts.
 *
 * `search.ts` now exports its own types directly, so this declaration
 * block is kept only for reference. It can be removed once all consumers
 * import from `@/markdoc/search` directly.
 */

// No ambient declarations required — search.ts is fully typed.
export {}
